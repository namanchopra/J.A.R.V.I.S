# Pipecat is the realtime voice pipeline framework powering the daemon. The
# silero / anthropic / local extras pull in Silero VAD + AnthropicLLMService +
# LocalAudioTransport — every voice loop touches all three.
pipecat-ai[silero,anthropic,local]>=1.0

websockets>=12.0
pyaudio>=0.2.14
mlx-whisper>=0.4
kokoro-onnx>=0.5
# vibevoice 1.0.0 is only on Microsoft's GitHub (PyPI's `vibevoice` is a
# different, unrelated package). Pin the exact commit so CI resolves the same
# wheel that local devs see.
vibevoice @ git+https://github.com/microsoft/VibeVoice.git@303b2833e01cff4578ec278bbfe536da54bd19fe
ollama>=0.4
anthropic>=0.40
openai>=1.0
numpy>=1.26
chromadb>=0.5
mcp>=1.0
